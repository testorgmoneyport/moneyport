/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // harici görsel kaynaklarını buraya ekliyoruz
  },
};

export default nextConfig;
